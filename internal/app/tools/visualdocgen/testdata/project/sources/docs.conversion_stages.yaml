apiVersion: leapview.dev/v1
kind: Source
metadata:
  id: source:docs.conversion_stages
  name: docs.conversion_stages
  displayName: Documentation conversion stages
spec:
  connection: docs
  location: {type: path, path: conversion_stages.csv, format: csv, options: {header: true}}
  schema: {mode: inferred}
