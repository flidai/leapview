apiVersion: leapview.dev/v1
kind: Source
metadata:
  id: source:docs.service_teams
  name: docs.service_teams
  displayName: Documentation service teams
spec:
  connection: docs
  location: {type: path, path: service_teams.csv, format: csv, options: {header: true}}
  schema: {mode: inferred}
