apiVersion: leapview.dev/v1
kind: Source
metadata:
  id: source:docs.revenue_bridge
  name: docs.revenue_bridge
  displayName: Documentation revenue bridge
spec:
  connection: docs
  location: {type: path, path: revenue_bridge.csv, format: csv, options: {header: true}}
  schema: {mode: inferred}
