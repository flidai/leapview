apiVersion: leapview.dev/v1
kind: Source
metadata:
  id: source:olist.order_items
  name: olist.order_items
  displayName: Order Items
spec:
  connection: olist
  path: olist_order_items_dataset.csv
  fields:
    order_id: {type: string, description: Order identifier for the item row.}
    product_id: {type: string, description: Product identifier purchased in the order.}
