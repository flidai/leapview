apiVersion: leapview.dev/v1
kind: Source
metadata:
  id: source:olist.payments
  name: olist.payments
  displayName: Payments
spec:
  connection: olist
  path: olist_order_payments_dataset.csv
  fields:
    order_id: {type: string, description: Order identifier for the payment.}
    payment_value: {type: number, description: Payment amount for the transaction.}
