apiVersion: leapview.dev/v1
kind: Source
metadata:
  id: source:links
  name: movielens.links
  displayName: External Links
spec:
  connection: movielens
  location:
    type: path
    path: links.csv
    format: csv
  schema:
    mode: compatible
    fields:
      movieId: {datatype: Integer, description: MovieLens movie identifier.}
      imdbId: {datatype: Integer, description: Numeric IMDb identifier without the tt prefix.}
      tmdbId: {datatype: Integer, description: Numeric TMDb movie identifier.}
