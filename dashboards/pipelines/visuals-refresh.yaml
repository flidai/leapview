apiVersion: leapview.dev/v1
kind: Pipeline
metadata:
  id: pipeline:visuals-refresh
  name: visuals-refresh
spec:
  selection:
    type: semanticModel
    semanticModel: semantic-model:visuals
  triggers:
    - id: manual
      type: manual
  runPolicy:
    overlap: replace
