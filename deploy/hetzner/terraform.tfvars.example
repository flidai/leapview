# Copy to terraform.tfvars for local use. Do not commit terraform.tfvars.

admin_email = "admin@example.com"

# Release image from the LeapView release notes. Immutable digests are required.
leapview_image = "ghcr.io/flidai/leapview@sha256:<digest-from-release>"

# Optional. If omitted, the module uses <reserved-ip>.sslip.io.
# domain = "leapview.example.com"

# Required: replace with your public IP range.
ssh_allowed_cidrs = ["203.0.113.10/32"]

# Optional: use an existing Hetzner SSH key instead of uploading your local key.
# ssh_key_ids = ["my-key"]
